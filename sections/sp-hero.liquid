{{ 'section-sp-hero.css' | asset_url | stylesheet_tag }}
{%- if template.suffix == 'supplement' -%}
  {{ 'sp-demo-supplement-hero.css' | asset_url | stylesheet_tag }}
{%- endif -%}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }
  .section-{{ section.id }}-padding.sp-hero-section--sticky {
    padding-bottom: calc({{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px + 7rem);
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

{%- liquid
  assign is_high_priority = false
  if section.settings.prioritize_lcp or section.index == 1
    assign is_high_priority = true
  endif

  assign image_loading = 'lazy'
  assign image_fetch_priority = 'auto'
  if is_high_priority
    assign image_loading = 'eager'
    assign image_fetch_priority = 'high'
  endif

  assign desktop_image = section.settings.hero_image_desktop
  if desktop_image == blank
    assign desktop_image = section.settings.hero_image
  endif

  assign pricing_product = section.settings.featured_product
  if pricing_product == blank and product != blank
    assign pricing_product = product
  endif

  if desktop_image == blank and pricing_product != blank and pricing_product.featured_image != blank
    assign desktop_image = pricing_product.featured_image
  endif

  assign mobile_image = section.settings.hero_image_mobile
  if mobile_image == blank
    assign mobile_image = desktop_image
  endif
  if mobile_image == blank and pricing_product != blank and pricing_product.featured_image != blank
    assign mobile_image = pricing_product.featured_image
  endif

  # SP hero fallback image resolution — vertical-agnostic, driven by sp-pack-registry.
  # Some demo / dev stores still surface Dawn placeholder art (snowboard, gift-card,
  # etc.) on the featured product. The registry supplies the detection tokens and the
  # replacement asset per vertical, so this core logic carries no vertical-specific
  # strings. Returns blank for verticals with no fallback configured, where this is a no-op.
  capture sp_hero_fallback_tokens
    render 'sp-pack-registry', field: 'hero_fallback_tokens'
  endcapture
  capture sp_hero_fallback_image
    render 'sp-pack-registry', field: 'hero_fallback_image'
  endcapture

  assign sp_hero_fallback = false
  if sp_hero_fallback_image != blank
    if mobile_image == blank
      assign sp_hero_fallback = true
    elsif sp_hero_fallback_tokens != blank
      assign sp_hero_probe = mobile_image | image_url: width: 100
      assign sp_hero_token_list = sp_hero_fallback_tokens | split: ','
      for sp_hero_token in sp_hero_token_list
        if sp_hero_probe contains sp_hero_token
          assign sp_hero_fallback = true
          break
        endif
      endfor
    endif
  endif

  assign hero_image_classes = 'sp-image'
  if settings.sp_image_progressive_reveal and sp_hero_fallback == false
    assign hero_image_classes = hero_image_classes | append: ' sp-image-reveal'
  endif

  if sp_hero_fallback
    assign desktop_image = blank
    assign mobile_image = blank
  endif

  assign alt_text = section.settings.image_alt | strip
  if alt_text == blank and desktop_image != blank
    assign alt_text = desktop_image.alt | escape
  endif

  if alt_text == blank and sp_hero_fallback
    assign alt_text = pricing_product.title | default: section.settings.headline | strip_html | escape
  endif

  if alt_text == blank
    assign alt_text = section.settings.headline | strip_html | escape
  endif

  assign heading_tag = section.settings.heading_tag | default: 'h1'

  assign mobile_ratio = 100
  if mobile_image != blank and mobile_image.aspect_ratio
    assign mobile_ratio = 1 | divided_by: mobile_image.aspect_ratio | times: 100
  elsif desktop_image != blank and desktop_image.aspect_ratio
    assign mobile_ratio = 1 | divided_by: desktop_image.aspect_ratio | times: 100
  endif

  assign desktop_ratio = mobile_ratio
  if desktop_image != blank and desktop_image.aspect_ratio
    assign desktop_ratio = 1 | divided_by: desktop_image.aspect_ratio | times: 100
  endif

  assign hero_focal_mobile = 'center'
  assign hero_focal_desktop = 'center'
  if mobile_image != blank and mobile_image.presentation.focal_point != blank
    assign hero_focal_mobile = mobile_image.presentation.focal_point
  endif
  if desktop_image != blank and desktop_image.presentation.focal_point != blank
    assign hero_focal_desktop = desktop_image.presentation.focal_point
  else
    assign hero_focal_desktop = hero_focal_mobile
  endif

  assign show_product_price = false
  if section.settings.price_source == 'product' and pricing_product != blank
    assign show_product_price = true
  endif

  assign benefit_chip_blocks = section.blocks | where: 'type', 'benefit_chip'
  assign trust_item_blocks = section.blocks | where: 'type', 'trust_item'

  assign show_hero_stats = false
  if section.settings.show_hero_stats
    if section.settings.stat_1_value != blank or section.settings.stat_2_value != blank or section.settings.stat_3_value != blank
      assign show_hero_stats = true
    endif
  endif

  assign show_floating_review = false
  if section.settings.show_floating_review and section.settings.floating_review_text != blank
    assign show_floating_review = true
  endif

  assign show_floating_shipping = false
  if section.settings.show_floating_shipping and section.settings.floating_shipping_text != blank
    assign show_floating_shipping = true
  endif

  assign show_floating_bestseller = false
  if section.settings.show_floating_bestseller and section.settings.floating_bestseller_text != blank
    assign show_floating_bestseller = true
  endif

  assign floating_review_rating = section.settings.floating_review_rating | default: 5 | plus: 0
  if floating_review_rating > 5
    assign floating_review_rating = 5
  elsif floating_review_rating < 1
    assign floating_review_rating = 1
  endif

  assign show_trust_microcopy = false
  if section.settings.show_trust_microcopy
    if section.settings.trust_microcopy_1 != blank or section.settings.trust_microcopy_2 != blank or section.settings.trust_microcopy_3 != blank
      assign show_trust_microcopy = true
    endif
  endif

  assign show_scroll_cue = false
  if section.settings.show_scroll_cue and section.settings.scroll_cue_text != blank and section.settings.scroll_cue_link != blank
    assign show_scroll_cue = true
  endif

  assign cta_product = pricing_product
  assign cta_type = section.settings.cta_type | default: 'custom_link'
  assign layout_preset = section.settings.layout_preset | default: 'classic_split'
  assign visual_effects = section.settings.visual_effects | default: 'none'

  assign show_mobile_sticky = false
  if section.settings.enable_mobile_sticky_cta
    assign show_mobile_sticky = true
  endif

  assign sticky_cta_label = section.settings.sticky_cta_label | strip
  if sticky_cta_label == blank
    assign sticky_cta_label = section.settings.cta_label
  endif

  assign load_product_form_js = false
  if cta_type == 'add_to_cart'
    assign load_product_form_js = true
  endif

  assign load_sp_hero_js = false
  if show_mobile_sticky or cta_type == 'buy_now'
    assign load_sp_hero_js = true
  endif
-%}

{% liquid
  assign sp_hero_extra_class = ''
  assign sp_hero_extra_attributes = ''
  if show_mobile_sticky
    assign sp_hero_extra_class = 'sp-hero-section--sticky'
    assign sp_hero_extra_attributes = 'data-sp-hero-sticky'
  endif
%}
{% render 'sp-section-hierarchy-open', section: section, extra_class: sp_hero_extra_class, extra_attributes: sp_hero_extra_attributes %}
    <div
      class="sp-hero sp-hero--image-{{ section.settings.desktop_image_position }} sp-hero--layout-{{ layout_preset }}"
      data-visual="{{ visual_effects }}"
      {% if section.settings.headline != blank %}
        aria-label="{{ section.settings.headline | strip_html | escape }}"
      {% else %}
        aria-label="Hero section"
      {% endif %}
    >
      <div class="sp-hero__content-wrapper">
        <div class="sp-hero__content sp-hero__content--{{ section.settings.content_alignment }} sp-hero__content--desktop-{{ section.settings.desktop_content_alignment }}{% render 'sp-motion-class', type: 'stagger' %}" data-sp-stagger>
          {%- if section.settings.show_announcement and section.settings.announcement_text != blank -%}
            <p class="sp-hero__announcement sp-hero__slot--announcement caption{% render 'sp-motion-class', type: 'stagger-item' %}">
              <span class="sp-hero__announcement-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
                  <path d="M12 3.5 4.5 7v6.2c0 4.5 2.8 8.6 7.5 9.8 4.7-1.2 7.5-5.3 7.5-9.8V7z"></path>
                  <path d="m8.8 12.1 2.2 2.3 4.3-4.4"></path>
                </svg>
              </span>
              {{ section.settings.announcement_text }}
            </p>
          {%- endif -%}

          {%- render 'sp-hero-social-proof', section: section -%}

          {%- if section.settings.eyebrow_text != blank -%}
            <p class="sp-hero__eyebrow sp-hero__slot--eyebrow caption-with-letter-spacing{% render 'sp-motion-class', type: 'stagger-item' %}">
              {{ section.settings.eyebrow_text }}
            </p>
          {%- endif -%}

          {%- if section.settings.badge_text != blank -%}
            <p class="sp-hero__badge sp-hero__slot--badge caption{% render 'sp-motion-class', type: 'stagger-item' %}">
              {{ section.settings.badge_text }}
            </p>
          {%- endif -%}

          {%- render 'sp-hero-headline', section: section, heading_tag: heading_tag -%}

          {%- if section.settings.subheadline != blank -%}
            <p class="sp-hero__subheadline sp-hero__slot--subheadline rte{% render 'sp-motion-class', type: 'stagger-item' %}">
              {{ section.settings.subheadline }}
            </p>
          {%- endif -%}

          {%- if benefit_chip_blocks.size > 0 -%}
            <ul class="sp-hero__benefits sp-hero__slot--benefits list-unstyled{% render 'sp-motion-class', type: 'stagger-item' %}" role="list">
              {%- for block in benefit_chip_blocks -%}
                {%- if block.settings.benefit_text != blank -%}
                  <li class="sp-hero__benefit caption" {{ block.shopify_attributes }}>
                    <span class="sp-hero__benefit-icon" aria-hidden="true">
                      {%- render 'sp-hero-benefit-icon', text: block.settings.benefit_text -%}
                    </span>
                    <span>{{ block.settings.benefit_text }}</span>
                  </li>
                {%- endif -%}
              {%- endfor -%}
            </ul>
          {%- endif -%}

          {%- if show_product_price -%}
            {%- assign price_variant = pricing_product.selected_or_first_available_variant -%}
            {%- if price_variant != blank -%}
              <p class="sp-hero__price sp-hero__slot--price subtitle{% render 'sp-motion-class', type: 'stagger-item' %}">
                {%- if price_variant.compare_at_price > price_variant.price -%}
                  <s class="sp-hero__price-compare">
                    {{ price_variant.compare_at_price | money }}
                  </s>
                {%- endif -%}

                <span class="sp-hero__price-current">
                  {{ price_variant.price | money }}
                </span>
              </p>
            {%- endif -%}
          {%- elsif section.settings.price_text != blank -%}
            <p class="sp-hero__price sp-hero__slot--price subtitle{% render 'sp-motion-class', type: 'stagger-item' %}">
              {{ section.settings.price_text }}
            </p>
          {%- endif -%}

          {%- if show_hero_stats -%}
            <ul class="sp-hero__stats sp-hero__slot--stats list-unstyled{% render 'sp-motion-class', type: 'stagger-item' %}" role="list" aria-label="Highlights">
              {%- if section.settings.stat_1_value != blank -%}
                <li class="sp-hero__stat">
                  <p class="sp-hero__stat-value h4">{{ section.settings.stat_1_value }}</p>
                  {%- if section.settings.stat_1_label != blank -%}
                    <p class="sp-hero__stat-label caption">{{ section.settings.stat_1_label }}</p>
                  {%- endif -%}
                </li>
              {%- endif -%}

              {%- if section.settings.stat_2_value != blank -%}
                <li class="sp-hero__stat">
                  <p class="sp-hero__stat-value h4">{{ section.settings.stat_2_value }}</p>
                  {%- if section.settings.stat_2_label != blank -%}
                    <p class="sp-hero__stat-label caption">{{ section.settings.stat_2_label }}</p>
                  {%- endif -%}
                </li>
              {%- endif -%}

              {%- if section.settings.stat_3_value != blank -%}
                <li class="sp-hero__stat">
                  <p class="sp-hero__stat-value h4">{{ section.settings.stat_3_value }}</p>
                  {%- if section.settings.stat_3_label != blank -%}
                    <p class="sp-hero__stat-label caption">{{ section.settings.stat_3_label }}</p>
                  {%- endif -%}
                </li>
              {%- endif -%}
            </ul>
          {%- endif -%}

          {%- if section.settings.cta_label != blank or cta_type == 'add_to_cart' or cta_type == 'buy_now' -%}
            <div class="sp-hero__cta sp-hero__slot--cta{% render 'sp-motion-class', type: 'stagger-item' %}" data-sp-hero-cta-anchor>
              {%- render 'sp-hero-cta',
                section: section,
                cta_product: cta_product,
                cta_label: section.settings.cta_label,
                cta_type: cta_type,
                cta_style: section.settings.cta_style,
                cta_link: section.settings.cta_link,
                cta_target_section: section.settings.cta_target_section
              -%}
            </div>
          {%- endif -%}

          {%- render 'sp-hero-trust-stack', section: section -%}

          {%- if show_trust_microcopy -%}
            <ul class="sp-hero__trust-microcopy sp-hero__slot--trust-microcopy list-unstyled{% render 'sp-motion-class', type: 'stagger-item' %}" role="list">
              {%- if section.settings.trust_microcopy_1 != blank -%}
                <li class="sp-hero__trust-microcopy-item caption">
                  <span class="sp-hero__trust-microcopy-icon" aria-hidden="true">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="m5.5 12.2 4.1 4.1 9-9"></path>
                    </svg>
                  </span>
                  {{ section.settings.trust_microcopy_1 | escape }}
                </li>
              {%- endif -%}
              {%- if section.settings.trust_microcopy_2 != blank -%}
                <li class="sp-hero__trust-microcopy-item caption">
                  <span class="sp-hero__trust-microcopy-icon" aria-hidden="true">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="m5.5 12.2 4.1 4.1 9-9"></path>
                    </svg>
                  </span>
                  {{ section.settings.trust_microcopy_2 | escape }}
                </li>
              {%- endif -%}
              {%- if section.settings.trust_microcopy_3 != blank -%}
                <li class="sp-hero__trust-microcopy-item caption">
                  <span class="sp-hero__trust-microcopy-icon" aria-hidden="true">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="m5.5 12.2 4.1 4.1 9-9"></path>
                    </svg>
                  </span>
                  {{ section.settings.trust_microcopy_3 | escape }}
                </li>
              {%- endif -%}
            </ul>
          {%- endif -%}

          {%- if section.settings.show_reassurance_row and trust_item_blocks.size > 0 -%}
            <ul class="sp-hero__trust-row sp-hero__slot--trust-row list-unstyled{% render 'sp-motion-class', type: 'stagger-item' %}" role="list">
              {%- for block in trust_item_blocks -%}
                {%- if block.settings.text != blank -%}
                  <li class="sp-hero__trust-item caption" {{ block.shopify_attributes }}>
                    <span class="sp-hero__trust-icon" aria-hidden="true">
                      {%- case block.settings.icon -%}
                        {%- when 'truck' -%}
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
                            <path d="M1.5 4.5h12v9h-12z"></path>
                            <path d="M13.5 8.5h4l3 3v2h-7z"></path>
                            <circle cx="6" cy="16.5" r="2"></circle>
                            <circle cx="17.5" cy="16.5" r="2"></circle>
                          </svg>
                        {%- when 'return' -%}
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
                            <path d="M7 7H3v4"></path>
                            <path d="M3 11a8.5 8.5 0 1 1 2.5 6"></path>
                            <path d="M8.5 20.5l-3 1.5"></path>
                          </svg>
                        {%- else -%}
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
                            <path d="M12 2.5 4.5 5.5v6.2c0 4.5 2.8 8.6 7.5 9.8 4.7-1.2 7.5-5.3 7.5-9.8V5.5z"></path>
                            <path d="m8.8 12.1 2.2 2.3 4.3-4.4"></path>
                          </svg>
                      {%- endcase -%}
                    </span>
                    {{ block.settings.text }}
                  </li>
                {%- endif -%}
              {%- endfor -%}
            </ul>
          {%- endif -%}

          {%- render 'sp-hero-logos', section: section -%}

          {%- if show_scroll_cue -%}
            <a href="{{ section.settings.scroll_cue_link }}" class="sp-hero__scroll-cue sp-hero__slot--scroll-cue caption{% render 'sp-motion-class', type: 'stagger-item' %}">
              <span>{{ section.settings.scroll_cue_text | escape }}</span>
              <span class="sp-hero__scroll-cue-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
                  <path d="M12 5v14"></path>
                  <path d="m6.5 13.5 5.5 5.5 5.5-5.5"></path>
                </svg>
              </span>
            </a>
          {%- endif -%}
        </div>
      </div>

      <div class="sp-hero__media-wrapper">
        <div
          class="sp-hero__media media{% if desktop_image == blank and mobile_image == blank and sp_hero_fallback == false %} placeholder{% endif %}"
          style="--sp-hero-ratio-mobile: {{ mobile_ratio | round: 2 }}%; --sp-hero-ratio-desktop: {{ desktop_ratio | round: 2 }}%; --sp-hero-focal-mobile: {{ hero_focal_mobile }}; --sp-hero-focal-desktop: {{ hero_focal_desktop }};"
        >
          <div class="sp-hero__media-inner">
            <div class="sp-hero__media-frame sp-media-frame sp-media-frame--custom{% render 'sp-motion-class', type: 'fade-in' %}">
              {%- render 'sp-hero-media',
                sp_hero_fallback: sp_hero_fallback,
                sp_hero_fallback_image: sp_hero_fallback_image,
                desktop_image: desktop_image,
                mobile_image: mobile_image,
                alt_text: alt_text,
                hero_image_classes: hero_image_classes,
                image_loading: image_loading,
                image_fetch_priority: image_fetch_priority
              -%}
            </div>

            {%- render 'sp-hero-floating-cards',
              section: section,
              show_floating_review: show_floating_review,
              show_floating_shipping: show_floating_shipping,
              show_floating_bestseller: show_floating_bestseller,
              floating_review_rating: floating_review_rating
            -%}
          </div>
        </div>
      </div>
    </div>

  {%- if show_mobile_sticky -%}
    <div
      class="sp-hero__sticky sp-hero__sticky--{{ section.settings.sticky_background_style | default: 'solid' }} {% render 'sp-section-hierarchy-surface', section: section %}"
      data-sp-hero-sticky-bar
      aria-hidden="true"
    >
      <div class="sp-hero__sticky-inner">
        {%- render 'sp-hero-cta',
          section: section,
          cta_product: cta_product,
          cta_label: sticky_cta_label,
          cta_type: cta_type,
          cta_style: section.settings.cta_style,
          cta_link: section.settings.cta_link,
          cta_target_section: section.settings.cta_target_section,
          is_sticky: true
        -%}
      </div>
    </div>
  {%- endif -%}
{% render 'sp-section-hierarchy-close' %}

{%- if load_product_form_js -%}
  <script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

{%- if load_sp_hero_js -%}
  <script src="{{ 'sp-hero.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

{% schema %}
{
  "name": "SP Hero",
  "tag": "section",
  "class": "section",
  "max_blocks": 20,
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "checkbox",
      "id": "show_announcement",
      "default": false,
      "label": "Show announcement bar"
    },
    {
      "type": "inline_richtext",
      "id": "announcement_text",
      "default": "Limited time: Free shipping on all orders",
      "label": "Announcement text"
    },
    {
      "type": "inline_richtext",
      "id": "eyebrow_text",
      "default": "Backed by 12,000+ customers",
      "label": "Eyebrow text"
    },
    {
      "type": "inline_richtext",
      "id": "headline",
      "default": "The smarter way to improve your daily routine",
      "label": "Headline"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "options": [
        {
          "value": "h1",
          "label": "H1"
        },
        {
          "value": "h2",
          "label": "H2"
        },
        {
          "value": "h3",
          "label": "H3"
        }
      ],
      "default": "h1",
      "label": "Heading tag"
    },
    {
      "type": "inline_richtext",
      "id": "subheadline",
      "default": "Designed for people who want premium results with less effort.",
      "label": "Subheadline"
    },
    {
      "type": "inline_richtext",
      "id": "badge_text",
      "default": "Best Seller",
      "label": "Badge text"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        { "value": "h1", "label": "Medium" },
        { "value": "h0", "label": "Large" },
        { "value": "hxl", "label": "XL" }
      ],
      "default": "h0",
      "label": "Headline size"
    },
    {
      "type": "select",
      "id": "headline_highlight_style",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "underline", "label": "Underline" },
        { "value": "background", "label": "Background" },
        { "value": "gradient", "label": "Gradient text" }
      ],
      "default": "none",
      "label": "Headline highlight style",
      "info": "Bold words in the headline are highlighted."
    },
    {
      "type": "color",
      "id": "headline_highlight_color",
      "label": "Headline highlight color"
    },
    {
      "type": "inline_richtext",
      "id": "price_text",
      "default": "From $49",
      "label": "Price text"
    },
    {
      "type": "product",
      "id": "featured_product",
      "label": "Featured product",
      "info": "Used for automatic pricing when price source is set to Product."
    },
    {
      "type": "select",
      "id": "price_source",
      "options": [
        {
          "value": "product",
          "label": "Product"
        },
        {
          "value": "custom",
          "label": "Custom text"
        }
      ],
      "default": "custom",
      "label": "Price source"
    },
    {
      "type": "header",
      "content": "Social proof"
    },
    {
      "type": "checkbox",
      "id": "show_social_proof",
      "default": false,
      "label": "Show social proof",
      "info": "Legacy text line. Prefer Rating value and Review count below for structured social proof."
    },
    {
      "type": "inline_richtext",
      "id": "social_proof_text",
      "default": "4.9â˜… Â· 12,000+ reviews",
      "label": "Social proof text"
    },
    {
      "type": "checkbox",
      "id": "show_rating_stars",
      "default": true,
      "label": "Show rating stars"
    },
    {
      "type": "checkbox",
      "id": "show_social_rating",
      "default": false,
      "label": "Show rating value"
    },
    {
      "type": "text",
      "id": "social_rating_value",
      "default": "4.9/5",
      "label": "Rating value"
    },
    {
      "type": "checkbox",
      "id": "show_social_review_count",
      "default": false,
      "label": "Show review count"
    },
    {
      "type": "text",
      "id": "social_review_count",
      "default": "12,000+ reviews",
      "label": "Review count"
    },
    {
      "type": "checkbox",
      "id": "show_social_customer_count",
      "default": false,
      "label": "Show customer count"
    },
    {
      "type": "text",
      "id": "social_customer_count",
      "default": "250K+ customers",
      "label": "Customer count"
    },
    {
      "type": "checkbox",
      "id": "show_social_avatars",
      "default": false,
      "label": "Show customer avatars"
    },
    {
      "type": "image_picker",
      "id": "social_avatar_1",
      "label": "Avatar 1"
    },
    {
      "type": "image_picker",
      "id": "social_avatar_2",
      "label": "Avatar 2"
    },
    {
      "type": "image_picker",
      "id": "social_avatar_3",
      "label": "Avatar 3"
    },
    {
      "type": "image_picker",
      "id": "social_avatar_4",
      "label": "Avatar 4"
    },
    {
      "type": "image_picker",
      "id": "social_avatar_5",
      "label": "Avatar 5"
    },
    {
      "type": "header",
      "content": "Benefits"
    },
    {
      "type": "paragraph",
      "content": "Add benefit blocks below."
    },
    {
      "type": "header",
      "content": "Hero statistics"
    },
    {
      "type": "checkbox",
      "id": "show_hero_stats",
      "default": false,
      "label": "Show hero statistics"
    },
    {
      "type": "text",
      "id": "stat_1_value",
      "default": "250K+",
      "label": "Statistic 1 value"
    },
    {
      "type": "text",
      "id": "stat_1_label",
      "default": "Customers",
      "label": "Statistic 1 label"
    },
    {
      "type": "text",
      "id": "stat_2_value",
      "default": "4.9/5",
      "label": "Statistic 2 value"
    },
    {
      "type": "text",
      "id": "stat_2_label",
      "default": "Rating",
      "label": "Statistic 2 label"
    },
    {
      "type": "text",
      "id": "stat_3_value",
      "default": "35+",
      "label": "Statistic 3 value"
    },
    {
      "type": "text",
      "id": "stat_3_label",
      "default": "Countries",
      "label": "Statistic 3 label"
    },
    {
      "type": "header",
      "content": "CTA"
    },
    {
      "type": "select",
      "id": "cta_type",
      "options": [
        { "value": "custom_link", "label": "Custom link" },
        { "value": "shop_now", "label": "Shop now" },
        { "value": "add_to_cart", "label": "Add to cart" },
        { "value": "buy_now", "label": "Buy now" },
        { "value": "scroll_to_section", "label": "Scroll to section" }
      ],
      "default": "custom_link",
      "label": "CTA type"
    },
    {
      "type": "text",
      "id": "cta_label",
      "default": "Shop now",
      "label": "CTA label"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA link",
      "info": "Used for Custom link and Shop now fallback."
    },
    {
      "type": "text",
      "id": "cta_target_section",
      "label": "CTA target section",
      "info": "Anchor link for Scroll to section, for example #shopify-section-template--1234567890__reviews."
    },
    {
      "type": "select",
      "id": "cta_style",
      "options": [
        { "value": "primary", "label": "Primary" },
        { "value": "secondary", "label": "Secondary" },
        { "value": "outline", "label": "Outline" }
      ],
      "default": "primary",
      "label": "CTA style"
    },
    {
      "type": "checkbox",
      "id": "enable_mobile_sticky_cta",
      "default": false,
      "label": "Enable mobile sticky CTA"
    },
    {
      "type": "text",
      "id": "sticky_cta_label",
      "label": "Sticky CTA label",
      "info": "Leave blank to reuse the main CTA label."
    },
    {
      "type": "select",
      "id": "sticky_background_style",
      "options": [
        { "value": "solid", "label": "Solid" },
        { "value": "blur", "label": "Blur" }
      ],
      "default": "solid",
      "label": "Sticky background style"
    },
    {
      "type": "header",
      "content": "Trust"
    },
    {
      "type": "checkbox",
      "id": "show_cta_trust_stack",
      "default": false,
      "label": "Show CTA trust stack",
      "info": "Add CTA trust items as blocks."
    },
    {
      "type": "checkbox",
      "id": "show_trust_microcopy",
      "default": true,
      "label": "Show trust microcopy below CTA"
    },
    {
      "type": "text",
      "id": "trust_microcopy_1",
      "default": "30-Day Money Back Guarantee",
      "label": "Trust line 1"
    },
    {
      "type": "text",
      "id": "trust_microcopy_2",
      "default": "Free Shipping Today",
      "label": "Trust line 2"
    },
    {
      "type": "text",
      "id": "trust_microcopy_3",
      "default": "Secure Checkout",
      "label": "Trust line 3"
    },
    {
      "type": "checkbox",
      "id": "show_reassurance_row",
      "default": true,
      "label": "Show reassurance row"
    },
    {
      "type": "checkbox",
      "id": "show_logos",
      "default": false,
      "label": "Show featured logos"
    },
    {
      "type": "text",
      "id": "logos_label",
      "default": "Featured In",
      "label": "Logos label"
    },
    {
      "type": "header",
      "content": "Scroll cue"
    },
    {
      "type": "checkbox",
      "id": "show_scroll_cue",
      "default": false,
      "label": "Show scroll cue"
    },
    {
      "type": "text",
      "id": "scroll_cue_text",
      "default": "See Why Customers Love It",
      "label": "Scroll cue text"
    },
    {
      "type": "url",
      "id": "scroll_cue_link",
      "label": "Scroll cue link",
      "info": "Link to the next section on the page, for example a reviews or benefits section."
    },
    {
      "type": "header",
      "content": "Floating cards"
    },
    {
      "type": "select",
      "id": "floating_card_style",
      "options": [
        { "value": "solid", "label": "Solid" },
        { "value": "glass", "label": "Glass" }
      ],
      "default": "solid",
      "label": "Floating card style"
    },
    {
      "type": "checkbox",
      "id": "show_floating_review",
      "default": false,
      "label": "Show floating review card"
    },
    {
      "type": "range",
      "id": "floating_review_rating",
      "min": 1,
      "max": 5,
      "step": 1,
      "default": 5,
      "label": "Floating review rating"
    },
    {
      "type": "text",
      "id": "floating_review_text",
      "default": "This changed my routine completely.",
      "label": "Floating review text"
    },
    {
      "type": "text",
      "id": "floating_review_author",
      "default": "Sarah M.",
      "label": "Floating review author"
    },
    {
      "type": "image_picker",
      "id": "floating_review_image",
      "label": "Floating review customer image"
    },
    {
      "type": "checkbox",
      "id": "show_floating_review_verified",
      "default": true,
      "label": "Show verified buyer badge"
    },
    {
      "type": "text",
      "id": "floating_review_verified_label",
      "default": "Verified Buyer",
      "label": "Verified buyer label"
    },
    {
      "type": "checkbox",
      "id": "show_floating_shipping",
      "default": false,
      "label": "Show floating shipping badge"
    },
    {
      "type": "text",
      "id": "floating_shipping_text",
      "default": "Free Shipping",
      "label": "Floating shipping text"
    },
    {
      "type": "checkbox",
      "id": "show_floating_bestseller",
      "default": false,
      "label": "Show floating bestseller badge"
    },
    {
      "type": "text",
      "id": "floating_bestseller_text",
      "default": "Bestseller",
      "label": "Floating bestseller text"
    },
    {
      "type": "header",
      "content": "Media"
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero image",
      "info": "Used as default image when device-specific images are not set."
    },
    {
      "type": "image_picker",
      "id": "hero_image_mobile",
      "label": "Mobile image"
    },
    {
      "type": "image_picker",
      "id": "hero_image_desktop",
      "label": "Desktop image"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Image alt text",
      "info": "Describe the image for screen readers."
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "layout_preset",
      "options": [
        { "value": "classic_split", "label": "Classic split" },
        { "value": "centered_conversion", "label": "Centered conversion" },
        { "value": "media_focus", "label": "Media focus" },
        { "value": "minimal", "label": "Minimal" },
        { "value": "authority", "label": "Authority" }
      ],
      "default": "classic_split",
      "label": "Layout preset"
    },
    {
      "type": "select",
      "id": "desktop_image_position",
      "options": [
        { "value": "right", "label": "Right" },
        { "value": "left", "label": "Left" }
      ],
      "default": "right",
      "label": "Desktop image position"
    },
    {
      "type": "select",
      "id": "content_alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" }
      ],
      "default": "left",
      "label": "Mobile content alignment"
    },
    {
      "type": "select",
      "id": "desktop_content_alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" }
      ],
      "default": "left",
      "label": "Desktop content alignment"
    },
{
    "type": "header",
    "content": "Visual hierarchy"
  },
  {
    "type": "select",
    "id": "sp_surface_style",
    "label": "Surface style",
    "default": "default",
    "info": "Inverse uses the inverse color scheme from Theme settings â†’ SP Visual hierarchy instead of the section Color scheme below.",
    "options": [
      {
        "value": "default",
        "label": "Default"
      },
      {
        "value": "alternate",
        "label": "Alternate"
      },
      {
        "value": "inverse",
        "label": "Inverse"
      },
      {
        "value": "accent",
        "label": "Accent"
      },
      {
        "value": "transparent",
        "label": "Transparent"
      }
    ]
  },
  {
    "type": "select",
    "id": "sp_width_mode",
    "label": "Width mode",
    "default": "contained",
    "options": [
      {
        "value": "contained",
        "label": "Contained"
      },
      {
        "value": "wide",
        "label": "Wide"
      },
      {
        "value": "full",
        "label": "Full width"
      },
      {
        "value": "bleed",
        "label": "Bleed"
      }
    ]
  },
  {
    "type": "select",
    "id": "sp_section_emphasis",
    "label": "Section emphasis",
    "default": "normal",
    "options": [
      {
        "value": "normal",
        "label": "Normal"
      },
      {
        "value": "elevated",
        "label": "Elevated"
      },
      {
        "value": "feature",
        "label": "Feature"
      }
    ]
  },
  {
    "type": "select",
    "id": "sp_divider_style",
    "label": "Divider",
    "default": "none",
    "options": [
      {
        "value": "none",
        "label": "None"
      },
      {
        "value": "line",
        "label": "Line"
      },
      {
        "value": "gradient",
        "label": "Gradient"
      },
      {
        "value": "fade",
        "label": "Fade"
      }
    ]
  },

    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "info": "Not applied when Surface style is set to Inverse.",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Visual effects"
    },
    {
      "type": "select",
      "id": "visual_effects",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "subtle", "label": "Subtle" },
        { "value": "premium", "label": "Premium" }
      ],
      "default": "none",
      "label": "Visual enhancement level"
    },
    {
      "type": "paragraph",
      "content": "Motion is controlled globally under Theme settings â†’ SP Motion. Reveal timing, stagger rhythm, and hover intensity apply to this section automatically."
    },
    {
      "type": "header",
      "content": "Performance"
    },
    {
      "type": "checkbox",
      "id": "prioritize_lcp",
      "default": true,
      "label": "Prioritize hero image loading",
      "info": "Enable for above-the-fold hero sections to improve Largest Contentful Paint."
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "benefit_chip",
      "name": "Benefit chip",
      "limit": 4,
      "settings": [
        {
          "type": "text",
          "id": "benefit_text",
          "default": "Better sleep",
          "label": "Benefit text"
        }
      ]
    },
    {
      "type": "trust_item",
      "name": "Trust item",
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "options": [
            { "value": "shield", "label": "Shield" },
            { "value": "truck", "label": "Truck" },
            { "value": "return", "label": "Return" }
          ],
          "default": "shield",
          "label": "Icon"
        },
        {
          "type": "text",
          "id": "text",
          "default": "Free shipping",
          "label": "Text"
        }
      ]
    },
    {
      "type": "cta_trust_item",
      "name": "CTA trust item",
      "limit": 5,
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "options": [
            { "value": "secure", "label": "Secure checkout" },
            { "value": "guarantee", "label": "Money back guarantee" },
            { "value": "shipping", "label": "Fast shipping" },
            { "value": "returns", "label": "Free returns" },
            { "value": "custom", "label": "Custom" }
          ],
          "default": "secure",
          "label": "Icon"
        },
        {
          "type": "text",
          "id": "text",
          "default": "Secure Checkout",
          "label": "Text"
        }
      ]
    },
    {
      "type": "logo",
      "name": "Logo",
      "limit": 6,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Logo image"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Logo link"
        },
        {
          "type": "checkbox",
          "id": "open_new_tab",
          "default": false,
          "label": "Open link in new tab"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "SP Hero",
      "settings": {
        "show_announcement": true,
        "show_trust_microcopy": true,
        "show_floating_review": true,
        "show_floating_review_verified": true,
        "show_floating_bestseller": true,
        "visual_effects": "none"
      },
      "blocks": [
        { "type": "benefit_chip", "settings": { "benefit_text": "Better Sleep" } },
        { "type": "benefit_chip", "settings": { "benefit_text": "Free Shipping" } },
        { "type": "benefit_chip", "settings": { "benefit_text": "Lifetime Warranty" } },
        { "type": "trust_item", "settings": { "icon": "truck", "text": "Free Shipping" } },
        { "type": "trust_item", "settings": { "icon": "return", "text": "Easy Returns" } },
        { "type": "trust_item", "settings": { "icon": "shield", "text": "Lifetime Warranty" } }
      ]
    },
    {
      "name": "Beauty Product",
      "settings": {
        "headline": "Glow-worthy skin in <strong>14 days</strong>",
        "headline_highlight_style": "underline",
        "subheadline": "Clinically inspired care for visible radiance without the routine overload.",
        "cta_type": "shop_now",
        "cta_label": "Shop the Serum",
        "show_cta_trust_stack": true,
        "show_social_rating": true,
        "show_social_review_count": true,
        "social_rating_value": "4.9/5",
        "social_review_count": "8,400+ reviews",
        "show_floating_review": true,
        "layout_preset": "centered_conversion",
        "content_alignment": "center",
        "desktop_content_alignment": "center",
        "visual_effects": "subtle"
      },
      "blocks": [
        { "type": "benefit_chip", "settings": { "benefit_text": "Dermatologist Tested" } },
        { "type": "benefit_chip", "settings": { "benefit_text": "Vegan Formula" } },
        { "type": "cta_trust_item", "settings": { "icon": "guarantee", "text": "30-Day Money Back Guarantee" } },
        { "type": "cta_trust_item", "settings": { "icon": "secure", "text": "Secure Checkout" } }
      ]
    },
    {
      "name": "Supplement",
      "settings": {
        "headline": "Daily energy backed by <strong>science</strong>",
        "headline_highlight_style": "background",
        "cta_type": "add_to_cart",
        "cta_label": "Add to Cart",
        "show_cta_trust_stack": true,
        "show_social_customer_count": true,
        "social_customer_count": "120K+ customers",
        "layout_preset": "authority"
      },
      "blocks": [
        { "type": "benefit_chip", "settings": { "benefit_text": "Third-Party Tested" } },
        { "type": "benefit_chip", "settings": { "benefit_text": "Made in USA" } },
        { "type": "cta_trust_item", "settings": { "icon": "guarantee", "text": "60-Day Guarantee" } },
        { "type": "cta_trust_item", "settings": { "icon": "shipping", "text": "Fast Shipping" } }
      ]
    },
    {
      "name": "Pet Product",
      "settings": {
        "headline": "The calm your pet deserves",
        "cta_type": "buy_now",
        "cta_label": "Buy Now",
        "show_floating_review": true,
        "show_floating_bestseller": true,
        "layout_preset": "classic_split",
        "visual_effects": "subtle"
      },
      "blocks": [
        { "type": "benefit_chip", "settings": { "benefit_text": "Vet Recommended" } },
        { "type": "cta_trust_item", "settings": { "icon": "returns", "text": "Free Returns" } }
      ]
    },
    {
      "name": "Electronics",
      "settings": {
        "headline": "Pro performance. Zero compromise.",
        "layout_preset": "media_focus",
        "cta_style": "outline",
        "show_logos": true,
        "logos_label": "Trusted By",
        "visual_effects": "premium"
      },
      "blocks": [
        { "type": "benefit_chip", "settings": { "benefit_text": "2-Year Warranty" } },
        { "type": "benefit_chip", "settings": { "benefit_text": "Free Express Shipping" } }
      ]
    },
    {
      "name": "Home Product",
      "settings": {
        "headline": "Elevate every room instantly",
        "layout_preset": "minimal",
        "cta_type": "scroll_to_section",
        "cta_label": "See It In Action"
      },
      "blocks": [
        { "type": "benefit_chip", "settings": { "benefit_text": "Easy Setup" } },
        { "type": "cta_trust_item", "settings": { "icon": "shipping", "text": "Free Shipping Today" } }
      ]
    },
    {
      "name": "Apparel",
      "settings": {
        "headline": "Fit that feels <strong>tailored</strong>",
        "headline_highlight_style": "gradient",
        "layout_preset": "centered_conversion",
        "content_alignment": "center",
        "desktop_content_alignment": "center",
        "cta_label": "Shop Collection",
        "floating_card_style": "glass",
        "show_floating_review": true
      },
      "blocks": [
        { "type": "benefit_chip", "settings": { "benefit_text": "Free Exchanges" } },
        { "type": "benefit_chip", "settings": { "benefit_text": "Premium Fabrics" } },
        { "type": "cta_trust_item", "settings": { "icon": "returns", "text": "Free Returns" } }
      ]
    }
  ]
}
{% endschema %}
