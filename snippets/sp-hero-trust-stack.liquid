{% comment %}
  CTA trust stack below hero button.
  Accepts: section
{% endcomment %}

{%- if section.settings.show_cta_trust_stack -%}
  {%- capture trust_row_class -%}sp-hero__trust-stack sp-hero__slot--trust-stack{% render 'sp-motion-class', type: 'stagger-item' %}{%- endcapture -%}
  {%- render 'sp-cta-trust-row', blocks: section.blocks, wrapper_class: trust_row_class -%}
{%- endif -%}
